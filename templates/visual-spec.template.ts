import { test } from '@playwright/test';

test.describe('[Feature] Visual', () => {
  test('captures governed visual evidence @visual', async ({ page }) => {
    void page;
    // Spec only orchestrates.
    // Use page objects, approved baselines, and reviewed scope.
  });
});
